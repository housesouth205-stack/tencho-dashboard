# 店長ダッシュボード — 作業の前提

パチンコ・パチスロ店の店長向け業務ダッシュボード。素のHTML/CSS/JS（ESモジュール、ビルド工程なし）＋
Supabase（PostgreSQL + 認証 + Edge Functions）、GitHub Pagesで配信。

`python -m http.server 8000` で `index.html` を開けば動く。トランスパイルもバンドルも無いので、
書いたコードがそのままブラウザで動く前提でよい。

---

## まず守ること

### 返答の最後に「今どうなっているか」を書く

コードを直しただけなのか、mainに入って本番（GitHub Pages）に出ているのかは、
店長側からは見分けがつかない。毎回、返答の最後に現状をまとめる:

- 何を変えたか（画面のどこが変わるか）
- 今どこにあるか（ブランチ止まり／PR作成済み／mainにマージ済み＝本番反映）
- 確認したこと・していないこと
- こちらから頼みたいこと（あれば）

### 作業を始める前にPRとmainの位置を確認する

このリポジトリでは追加要望が連続して来る。**前回のPRが既にマージされていることがある。**
マージ済みブランチにコミットを足しても永久に反映されない（実際に2回起きた）。

```bash
git fetch origin main && git log --oneline -1 origin/main
# 直前のPRがマージ済みなら、mainから作り直してから作業する
git checkout -B <branch> origin/main
```

「PRを更新しました」と報告する前に、そのPRが本当にopenかを確認する。

### 一括置換はアンカーの一致を確認する

`sed` 相当の文字列置換は、対象が無くても**黙って何もしない**。存在しない目印を頼りに定義行を
挿入しようとして失敗し、未定義参照で実行中の処理が落ちたことがある。
Edit ツール（一致しなければ失敗する）を優先し、スクリプトで置換したら必ず grep で結果を確認する。

### プロセスを止めるときのパターンに注意

`pkill -f collect.py` のようなパターンは**自分のシェルのコマンドラインにも一致**して、
自分ごと落ちる。PIDを特定して kill する。

---

## コードの書き方

### コメントは「なぜ」を日本語で書く

このリポジトリのコメントは、何をしているかではなく**なぜそうしたか**を書いている。
既存のコードを読めば分かる: 「0で描くと未入力日が売上ゼロの日に見える」「2本並べると1本3px未満になる」。
同じ密度・同じ調子で書く。英語のコメントは既存コードに無い。

### 色の使い分け（全画面で統一）

| 用途 | 色 |
|---|---|
| 売上 | 青 `#4f8ff7` |
| 粗利 | 緑 `#2fb888` |
| 計画（グループ） | 鋼青 `#6b7f9e` |
| 実績（グループ） | 深緑 `#1f9d70` |
| 前年（参考） | グレー `#8a91a3` |
| 達成: 100%以上／90%以上／未満 | 緑 `#43b483`／黄 `#e0a52e`／赤 `#e35d6a` |

区分（20スロ/5スロ/2スロ）の色は `util/colors.js` の `sectionColor()`。

### 表とスマホ

列の多い表は `.table-wrap` で表の中だけ横スクロールさせる。
`window.matchMedia("(max-width: 700px)")` が真のときはカード表示に切り替える実装が
`yojitsu/view.js` と `yojitsu/dailyTable.js` にあるので、それに倣う。

### 欠損は0で埋めない

**未入力と0は別物。** 実績が無い日を0として描くと「売上ゼロの日」に見える。
設定の空欄は「その機種にその設定が無い」という意味で、シミュレーターはこれを見て
存在しない設定を割り当てないようにしている。空欄には意味があるので勝手に埋めない。

---

## 画面ごとの要点

### 設定投入シミュレーター `src/features/simulator/`

- 保存先は `sim_session(target_date, allocation)`。`allocation.assign` = `{区分キー: {台番: 設定}}`
- **保存が無い日は「対象日より前の最新の保存」を引き継ぐ。** 実機は触らなければ設定が残るので、
  毎日ゼロから始めるのは運用と合わない。休業日を挟むため「前日ちょうど」に限定してはいけない
- `allocation.hold` = 据え置き指定 `{区分キー: {台番: true}}`。一括操作（実績で選んで投入・
  全台リセット）の対象外。**個別タップでは変更できる**（完全ロックだと解除を忘れると詰む）
- 「全台を最低設定に戻す」は画面上だけ。以降の日の保存は消さないので、引き継ぎは古い設定を拾い続ける。
  断ち切るのは「この日から仕切り直す」（削除対象の日付を一覧で見せてから実行）
- 未保存の変更がある状態で日付を移動するときは必ず確認する。黙って破棄すると事故になる

### 出玉率管理 `src/features/payout/`

- `model_spec(model_name, setting, payout_rate, source, source_url)` に保存
- 埋め方は3つ: 手動 ／ 📚 機種DB（同梱JSON、通信不要） ／ 🌐 Web取得（一撃・DMM）
- **店舗の遊技台CSVの機種名は検定型式名**（例 `S/新ﾊﾅﾋﾞR/HA`）。機種名だけで照合すると当たらない。
  `localdb.js` は機種名・別表記・型式名すべてを照合キーにしている
- 一括適用は一致率30%以上で自動確定。完全一致でないものは状態列に一致率を橙色で出す

### 予実管理 `src/features/yojitsu/`

- `plan_day` / `actual_day` / `machines_day` を `(ymd|section_id)` のMapで持つ
- 集計は `src/calc/aggregate.js`。月サマリー・日次系列・日別明細
- **達成率を出すときは分母に注意。** 実績20日ぶんを計画31日ぶんで割ると未達に見える。
  実績のある日の計画（`planElapsed` / `pe`）と比べる

---

## 機種データベース `pachislot-db/`

スマスロ・6号機の出玉率を出典付きで集めたもの。詳細は `pachislot-db/README.md`。

### 絶対のルール: 推測しない

- 範囲表記（`97.0〜113.0%`）からは**設定1と設定6だけ**記録。設定2〜5は空欄のまま。内挿しない
- 出典URLの無い数値は記録しない
- 情報源が食い違ったら平均も選択もせず、両方の出典を残して「要確認」
- 規則区分はP-WORLDのタイプ欄、現行判定は設置店舗数。導入年から推定しない

### 外部依存は `requests` のみ

パッケージレジストリが遮断された環境だったため、`pandas`/`openpyxl`/`beautifulsoup4`/`lxml` は
標準ライブラリ実装（`minihtml.py`／`minixlsx.py`）に置き換えてある。**元に戻さない。**

### 派生物の作り直し忘れを検出する

原本 `data/machines.json` を変えたら、そこから派生するもの（`output/`・
`src/data/payout-db.json`・READMEの実績値）も作り直す。手で覚えておく話にせず、

```bash
python3 pachislot-db/scripts/check_all.py --fix
```

を通す。READMEの実績値は `readme_stats.py` が生成するので**手で書き換えない**
（手打ちしていた頃に2回古いまま出した）。

### スクレイピング

robots.txt を必ず確認し、同一ホストへ**最低2.5秒間隔・並列なし**。取得したHTMLは `cache/` に
残るので再実行してもサイトに再アクセスしない（`cache/` は追跡しない、`candidates/` は追跡する）。

---

## 検証のやり方

CIは無い。手元で確認してからコミットする。

```bash
node --check <file.js>                      # 構文
python3 pachislot-db/scripts/check_all.py   # DBのテスト＋派生物のズレ
```

**画面の変更はChromiumで実際に描画して確認する。** Playwrightは未インストールだがChromiumは同梱:

```bash
/opt/pw-browsers/chromium-1194/chrome-linux/chrome --headless=new --no-sandbox \
  --virtual-time-budget=9000 --dump-dom http://localhost:8000/<test>.html
# --screenshot=/tmp/x.png で見た目も確認できる
```

Supabaseに繋がらなくても、合成データを渡して描画モジュールだけ動かせば検証できる
（`aggregate.js` や `miniMap.js` は repo/state に依存していない）。

---

## Git

- 作業ブランチは指定されたものを使う。mainへ直接コミットしない
- PRは明示的に頼まれるまで作らない
- コミットメッセージは英語、本文で**なぜ・何を検証したか**を書く。既存のログに倣う
- 生成物（`output/`・`src/data/payout-db.json`）はコミットに含める。実行環境の都合で
  再生成できないことがあるため
